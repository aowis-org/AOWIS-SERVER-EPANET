diff --git a/include/aowis/epanet/epanet.h b/include/aowis/epanet/epanet.h
index 6fb5de8..bc05041 100644
--- a/include/aowis/epanet/epanet.h
+++ b/include/aowis/epanet/epanet.h
@@ -1,12 +1,11 @@
 #ifndef AOWIS_EPANET_H
 #define AOWIS_EPANET_H
 
+#include <aowis/epanet/dummy/dummy_networks.h>
 #include <aowis/epanet/epanet_resolvers.h>
 #include <aowis/epanet/epanet_run_result.h>
 #include <aowis/epanet/epanet_runner.h>
 #include <aowis/epanet/epanet_simulation_manager.h>
-#include <aowis/epanet/epanet_wrapper.h>
-#include <aowis/epanet/dummy/dummy_networks.h>
 #include <aowis/epanet/utility/simulation_result_printer.h>
 #include <aowis/epanet/utility/simulation_status_printer.h>
 
diff --git a/src/lib/CMakeLists.txt b/src/lib/CMakeLists.txt
index b865259..c2afdb4 100644
--- a/src/lib/CMakeLists.txt
+++ b/src/lib/CMakeLists.txt
@@ -2,7 +2,6 @@ set(AOWIS_SERVER_EPANET_SOURCES
     src/lib/epanet_resolvers.cpp
     src/lib/epanet_runner.cpp
     src/lib/epanet_simulation_manager.cpp
-    src/lib/epanet_wrapper.cpp
     src/lib/dummy/dummy_networks.cpp
     src/lib/utility/simulation_result_printer.cpp
     src/lib/utility/simulation_status_printer.cpp
@@ -21,7 +20,6 @@ set(AOWIS_SERVER_EPANET_PUBLIC_HEADERS
     include/aowis/epanet/epanet_run_result.h
     include/aowis/epanet/epanet_runner.h
     include/aowis/epanet/epanet_simulation_manager.h
-    include/aowis/epanet/epanet_wrapper.h
     include/aowis/epanet/dummy/dummy_networks.h
     include/aowis/epanet/utility/simulation_result_printer.h
     include/aowis/epanet/utility/simulation_status_printer.h
@@ -37,45 +35,36 @@ set(AOWIS_SERVER_EPANET_INTERNAL_HEADERS
     src/lib/internal/epanet_status_helpers.h
 )
 
-add_library(aowis-server-epanet INTERFACE)
-add_library(AOWIS::ServerEpanet ALIAS aowis-server-epanet)
-add_library(AOWIS::Epanet ALIAS aowis-server-epanet)
-
-add_library(aowis-server-epanet-core STATIC
+add_library(aowis-server-epanet STATIC
     ${AOWIS_SERVER_EPANET_SOURCES}
     ${AOWIS_SERVER_EPANET_PUBLIC_HEADERS}
     ${AOWIS_SERVER_EPANET_INTERNAL_HEADERS}
 )
 
-source_group(
-    TREE "${PROJECT_SOURCE_DIR}"
-    FILES
-        ${AOWIS_SERVER_EPANET_SOURCES}
-        ${AOWIS_SERVER_EPANET_PUBLIC_HEADERS}
-        ${AOWIS_SERVER_EPANET_INTERNAL_HEADERS}
+add_library(AOWIS::ServerEpanet ALIAS aowis-server-epanet)
+
+source_group(TREE "${PROJECT_SOURCE_DIR}" FILES
+    ${AOWIS_SERVER_EPANET_SOURCES}
+    ${AOWIS_SERVER_EPANET_PUBLIC_HEADERS}
+    ${AOWIS_SERVER_EPANET_INTERNAL_HEADERS}
 )
 
-target_include_directories(aowis-server-epanet-core
+target_include_directories(aowis-server-epanet
     PUBLIC
         "${PROJECT_SOURCE_DIR}/include"
     PRIVATE
         "${PROJECT_SOURCE_DIR}/src/lib"
 )
 
-target_link_libraries(aowis-server-epanet-core
+target_link_libraries(aowis-server-epanet
     PUBLIC
         Qt6::Core
         AOWIS::ServerModel
         epanet2
 )
 
-target_compile_features(aowis-server-epanet-core PUBLIC cxx_std_20)
+target_compile_features(aowis-server-epanet PUBLIC cxx_std_20)
 
-set_target_properties(aowis-server-epanet-core PROPERTIES
+set_target_properties(aowis-server-epanet PROPERTIES
     POSITION_INDEPENDENT_CODE ON
 )
-
-target_link_libraries(aowis-server-epanet
-    INTERFACE
-        aowis-server-epanet-core
-)
diff --git a/src/server/CMakeLists.txt b/src/server/CMakeLists.txt
index 2ea1ebe..a2a5c8f 100644
--- a/src/server/CMakeLists.txt
+++ b/src/server/CMakeLists.txt
@@ -5,4 +5,11 @@ qt_add_executable(aowis-server-epanet-server
 )
 
 set_target_properties(aowis-server-epanet-server PROPERTIES OUTPUT_NAME "aowis-server-epanet")
-target_link_libraries(aowis-server-epanet-server PRIVATE aowis-server-epanet-core Qt6::Core Qt6::Network Qt6::HttpServer)
+
+target_link_libraries(aowis-server-epanet-server
+    PRIVATE
+        AOWIS::ServerEpanet
+        Qt6::Core
+        Qt6::Network
+        Qt6::HttpServer
+)
diff --git a/src/server/server.cpp b/src/server/server.cpp
index b332d32..e41cba9 100644
--- a/src/server/server.cpp
+++ b/src/server/server.cpp
@@ -1,9 +1,10 @@
 #include "server.h"
+
 #include <QDebug>
 #include <QHostAddress>
 #include <QHttpServerResponse>
 #include <aowis/epanet/dummy/dummy_networks.h>
-#include <aowis/epanet/epanet_wrapper.h>
+#include <aowis/epanet/epanet_runner.h>
 #include <aowis/epanet/utility/simulation_result_printer.h>
 #include <aowis/epanet/utility/simulation_status_printer.h>
 
@@ -12,19 +13,22 @@ Server::Server(QCoreApplication *app, QObject *parent)
 {
     setupRoutes();
 }
+
 void Server::setupRoutes()
 {
-    NetworkHydraulic network = DummyNetworks::networkTanks();
-    EpanetWrapper epanet;
-    const SimulationResultTimeline result = epanet.run(network);
-    SimulationStatusPrinter::print(result.status);
-    SimulationResultPrinter::print(result);
-    qDebug().noquote() << epanet.reportText();
+    const NetworkHydraulic network = DummyNetworks::networkTanks();
+    EpanetRunner runner;
+    const EpanetRunResult run_result = runner.run(network);
+
+    SimulationStatusPrinter::print(run_result.timeline.status);
+    SimulationResultPrinter::print(run_result.timeline);
+    qDebug().noquote() << run_result.report.join('\n');
 
     this->http.route("/status", []()
     {
         return QHttpServerResponse("AOWIS EPANET server", QHttpServerResponse::StatusCode::Ok);
     });
+
     this->tcp = new QTcpServer(this->app);
     if (!this->tcp->listen(QHostAddress::Any, 8122))
     {
